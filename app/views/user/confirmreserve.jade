html
  head
    title=title
    link(href='/css/main.css', rel='stylesheet')
  body
    h3 Confirm?, #{username}
    div
      p #{restaurant.name} (#{restaurant.description})
      a(href='/restaurant/reserving/confirm/' + restaurant.name) Reserve
      br
      a(href='/restaurant/' + restaurant.name) Cancel
